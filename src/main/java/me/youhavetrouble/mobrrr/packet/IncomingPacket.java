package me.youhavetrouble.mobrrr.packet;

/**
 * Represents a packet that is incoming from the client.<br>
 * Implementations need to have a constructor that takes a {@link java.io.DataInputStream} as an argument.
 */
public abstract class IncomingPacket extends Packet {
}
